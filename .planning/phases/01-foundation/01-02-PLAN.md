---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/envfile/types.go
  - internal/envfile/reader.go
  - internal/envfile/writer.go
  - internal/envfile/reader_test.go
  - internal/envfile/writer_test.go
autonomous: true
requirements:
  - DISC-04
  - ROT-03

must_haves:
  truths:
    - "Tool reads .env files preserving comments, blank lines, and quoting styles"
    - "Tool reads multiple .env files independently (multi-file support)"
    - "Tool writes .env files atomically via temp-file + sync + rename"
    - "Tool preserves original file permissions after atomic write"
    - "Modified values retain their original quoting style"
  artifacts:
    - path: "internal/envfile/types.go"
      provides: "Line and EnvFile structs"
      exports: ["Line", "EnvFile"]
    - path: "internal/envfile/reader.go"
      provides: ".env file parser preserving formatting"
      exports: ["Read"]
    - path: "internal/envfile/writer.go"
      provides: "Atomic .env file writer and Set method"
      exports: ["WriteAtomic", "Set"]
  key_links:
    - from: "internal/envfile/writer.go"
      to: "filesystem"
      via: "os.CreateTemp + f.Sync + os.Rename"
      pattern: "os\\.Rename"
    - from: "internal/envfile/reader.go"
      to: "internal/envfile/types.go"
      via: "parseLine returns Line structs"
      pattern: "parseLine"
---

<objective>
Build the .env file reader/writer package with line-level editing and atomic writes.

Purpose: .env file handling is the core data layer -- every rotation reads and writes .env files. Atomic writes prevent corruption on crash. Line-level editing preserves comments and formatting that users rely on.
Output: A tested envfile package that reads, modifies, and atomically writes .env files while preserving all formatting.
</objective>

<execution_context>
@/home/giulio/.claude/get-shit-done/workflows/execute-plan.md
@/home/giulio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From plan 01-01, the go.mod and project structure will exist. This plan only needs the Go module to be initialized. No imports from internal/config or internal/cli are needed. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Envfile types and reader</name>
  <files>internal/envfile/types.go, internal/envfile/reader.go, internal/envfile/reader_test.go</files>
  <behavior>
    - TestReadSimpleKeyValue: Reads `KEY=value` lines correctly, Key and Value populated
    - TestReadDoubleQuoted: Reads `KEY="value with spaces"` preserving Quoted=`"` and unquoting the value
    - TestReadSingleQuoted: Reads `KEY='value'` preserving Quoted=`'`
    - TestReadComments: Comment lines (starting with #) have Comment=true, Key=""
    - TestReadBlankLines: Blank lines preserved with empty Raw, Key=""
    - TestReadMalformed: Lines without = sign preserved as-is (Raw intact, Key="")
    - TestReadInlineComment: `KEY=value # comment` -- the value includes everything after = (Go .env convention: no inline comments unless quoted)
    - TestReadExportPrefix: `export KEY=value` -- handle `export ` prefix, Key="KEY"
    - TestReadMultipleFiles: Read two separate .env files, each returns independent EnvFile
    - TestGet: EnvFile.Get(key) returns value via O(1) index lookup
    - TestGetMissing: EnvFile.Get(nonexistent) returns "", false
    - TestReadPreservesOriginalLines: Round-trip Raw lines match original file content exactly
    - TestReadHashInQuotedValue: `KEY="pass#word"` -- hash inside quotes is part of value, not a comment
  </behavior>
  <action>
    1. Create `internal/envfile/types.go` exactly per research Pattern 3:
       - Line struct: Raw string, Key string, Value string, Quoted string, Comment bool
       - EnvFile struct: Path string, Lines []Line, index map[string]int
       - Get(key string) (string, bool) method on EnvFile using index
       - Keys() []string method returning all keys in file order
    2. Create `internal/envfile/reader.go` exactly per research Pattern 3:
       - Read(path string) (*EnvFile, error) function
       - parseLine(raw string) Line function handling:
         - Blank lines (trimmed == "")
         - Comment lines (starts with #)
         - KEY=VALUE (unquoted)
         - KEY="VALUE" (double quoted, Quoted=`"`)
         - KEY='VALUE' (single quoted, Quoted=`'`)
         - export KEY=VALUE (strip export prefix)
         - Malformed lines (no = sign, preserved as-is)
       - Build index map for O(1) key lookups
    3. Create `internal/envfile/reader_test.go` with all behaviors above
       - Use t.TempDir() to create test .env files
       - Use testify/assert for assertions
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go test ./internal/envfile/... -run "TestRead|TestGet" -v -count=1</automated>
  </verify>
  <done>EnvFile reader parses all .env formats (quoted, unquoted, comments, blanks, export prefix) with O(1) key lookup. All reader tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic envfile writer</name>
  <files>internal/envfile/writer.go, internal/envfile/writer_test.go</files>
  <behavior>
    - TestSetUpdatesValue: Set("KEY", "newval") updates the value in the Line and reconstructs Raw
    - TestSetPreservesDoubleQuotes: Set on a double-quoted key preserves double quote style in Raw
    - TestSetPreservesSingleQuotes: Set on a single-quoted key preserves single quote style in Raw
    - TestSetNonExistentKey: Set on a key not in the file is a no-op (no crash, no new line)
    - TestWriteAtomicCreatesFile: WriteAtomic writes all lines to the target path
    - TestWriteAtomicPreservesComments: After WriteAtomic, comments and blank lines are preserved byte-for-byte
    - TestWriteAtomicPreservesPermissions: Original file permissions (e.g., 0600) are preserved after write
    - TestWriteAtomicNoCorruptionOnSimulatedCrash: Temp file exists during write; target file remains intact if rename is not called (simulated by checking temp file creation in same dir)
    - TestWriteAtomicRoundTrip: Read -> Set -> WriteAtomic -> Read produces expected modified values while preserving all other lines exactly
  </behavior>
  <action>
    1. Create `internal/envfile/writer.go` exactly per research Pattern 4:
       - Set(key, newValue string) method on *EnvFile:
         - Look up key in index
         - If found, update Value and reconstruct Raw preserving Quoted style
         - If not found, no-op (do not append -- the caller should know the key exists)
       - WriteAtomic() error method on *EnvFile:
         - os.Stat to get original permissions
         - os.CreateTemp in same directory as ef.Path (critical for atomic rename on same filesystem)
         - Write all lines joined by newline
         - f.Sync() to flush to disk
         - f.Close()
         - os.Chmod temp file to original permissions
         - os.Rename temp to ef.Path (atomic operation)
         - defer os.Remove(tmpPath) for cleanup on any error path
    2. Create `internal/envfile/writer_test.go` with all behaviors above
       - Use t.TempDir() for isolated test files
       - Create test .env files with known content, permissions
       - For round-trip test: Read -> Set -> WriteAtomic -> Read again -> assert values and formatting
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go test ./internal/envfile/... -v -count=1</automated>
  </verify>
  <done>Atomic writer creates temp file in same directory, syncs, renames. Preserves permissions and formatting. All envfile tests pass (reader + writer).</done>
</task>

</tasks>

<verification>
- `go test ./internal/envfile/... -v -count=1` -- all tests pass
- `go test ./internal/envfile/... -race -count=1` -- no race conditions
- `go vet ./internal/envfile/...` -- no issues
</verification>

<success_criteria>
- .env files with comments, blank lines, quoted values, export prefix all parse correctly
- Set() preserves quoting style (double, single, none)
- WriteAtomic uses temp-file + sync + rename pattern (ROT-03)
- File permissions preserved after atomic write
- Multiple .env files can be read independently (DISC-04)
- Full round-trip (read -> modify -> write -> read) preserves formatting
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
