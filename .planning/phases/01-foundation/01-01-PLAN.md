---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/rotator/main.go
  - internal/cli/root.go
  - internal/cli/scan.go
  - internal/cli/rotate.go
  - internal/cli/status.go
  - internal/cli/history.go
  - internal/config/config.go
  - internal/config/validation.go
  - internal/config/defaults.go
  - internal/config/config_test.go
  - rotator.example.yml
autonomous: true
requirements:
  - DIST-03
  - DISC-03

must_haves:
  truths:
    - "User can run `rotator --help` and see scan, rotate, status, history subcommands"
    - "Tool loads and validates a rotator.yml configuration file with clear errors for invalid config"
    - "Tool works zero-config when no rotator.yml exists (returns default config, no error)"
  artifacts:
    - path: "cmd/rotator/main.go"
      provides: "CLI entry point"
      contains: "cli.NewRootCmd"
    - path: "internal/cli/root.go"
      provides: "Root command with global flags and subcommands"
      exports: ["NewRootCmd"]
    - path: "internal/config/config.go"
      provides: "Config struct and koanf-based loader"
      exports: ["Config", "SecretConfig", "Load"]
    - path: "internal/config/validation.go"
      provides: "Config validation rules"
      exports: ["validate"]
    - path: "rotator.example.yml"
      provides: "Example configuration file"
  key_links:
    - from: "cmd/rotator/main.go"
      to: "internal/cli/root.go"
      via: "cli.NewRootCmd().Execute()"
      pattern: "cli\\.NewRootCmd"
    - from: "internal/config/config.go"
      to: "koanf"
      via: "koanf.Load with optional file provider"
      pattern: "koanf\\.New"
---

<objective>
Set up the Go project scaffold, CLI skeleton with Cobra, and configuration loading with koanf.

Purpose: Establish the foundational project structure that all subsequent plans build on. The CLI and config loader are needed by every feature.
Output: A runnable `rotator` binary with --help, four stub subcommands, and YAML config loading that works with or without a config file.
</objective>

<execution_context>
@/home/giulio/.claude/get-shit-done/workflows/execute-plan.md
@/home/giulio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold and config package</name>
  <files>go.mod, internal/config/config.go, internal/config/validation.go, internal/config/defaults.go, internal/config/config_test.go, rotator.example.yml</files>
  <behavior>
    - TestLoadYAML: Loading a valid rotator.yml returns a populated Config struct with correct secret definitions
    - TestLoadNoFile: Loading with empty path returns default config (no error), zero-config mode (DISC-03)
    - TestLoadInvalidType: Config with invalid secret type returns descriptive validation error
    - TestLoadMissingName: Config with missing secret name returns descriptive validation error
    - TestLoadMissingEnvKey: Config with missing env_key returns descriptive validation error
    - TestLoadMissingEnvFile: Config with neither env_file nor env_files returns descriptive validation error
    - TestLoadEnvVarOverride: ROTATOR_ prefixed env vars override config file values
    - TestMultiFileEnvConfig: Config with env_files array (multiple .env paths) loads correctly (DISC-04 config support)
  </behavior>
  <action>
    1. Initialize Go module: `go mod init github.com/giulio/secret-rotator`
    2. Install Phase 1 dependencies per research:
       - `go get github.com/spf13/cobra@v1.10.2`
       - `go get github.com/knadh/koanf/v2@latest`
       - `go get github.com/knadh/koanf/parsers/yaml@latest`
       - `go get github.com/knadh/koanf/providers/file@latest`
       - `go get github.com/knadh/koanf/providers/env@latest`
       - `go get github.com/stretchr/testify@latest`
    3. Create `internal/config/config.go` with:
       - Config struct: MasterKeyEnv string, Secrets []SecretConfig, Notifications []NotifyConfig
       - SecretConfig struct: Name, Type, EnvKey, EnvFile string, EnvFiles []string, Containers []string, Provider map[string]string, Schedule string, Length int (all with koanf tags)
       - NotifyConfig struct: Type, URL string
       - Load(path string) (*Config, error) function using koanf:
         - Create koanf instance with "." delimiter
         - If path != "", load YAML file via file.Provider + yaml.Parser
         - If path != "" and file doesn't exist, return error (user explicitly specified a file)
         - Always overlay ROTATOR_ prefixed env vars via env.Provider
         - Unmarshal to Config struct
         - Run validate()
         - Return config
    4. Create `internal/config/defaults.go` with:
       - DefaultConfig() *Config returning sensible defaults (empty secrets list, no notifications)
       - This is what Load returns when called with empty path (zero-config mode)
    5. Create `internal/config/validation.go` with validate() function per research Pattern (config validation):
       - Each secret must have a Name
       - Type if set must be one of: mysql, postgres, redis, generic
       - EnvKey is required
       - Either EnvFile or EnvFiles (non-empty) is required
    6. Create `rotator.example.yml` with the example config from research (mysql, redis, generic secret examples with notifications)
    7. Create test file `internal/config/config_test.go` with all behaviors listed above
       - Use testify/assert and testify/require
       - Use t.TempDir() for test config files
       - Write test YAML files to temp dirs
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go test ./internal/config/... -v -count=1</automated>
  </verify>
  <done>Config package loads YAML via koanf, validates schema, supports zero-config mode, overlays env vars. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CLI skeleton with Cobra</name>
  <files>cmd/rotator/main.go, internal/cli/root.go, internal/cli/scan.go, internal/cli/rotate.go, internal/cli/status.go, internal/cli/history.go</files>
  <action>
    1. Create `internal/cli/root.go` with NewRootCmd() per research Pattern 1:
       - Use: "rotator", Short description, Long description
       - PersistentFlags: --config (string, default ""), --verbose (bool), --dry-run (bool)
       - Add all four subcommands via AddCommand
       - PersistentPreRunE: Load config using the --config flag value. If --config is empty, call config.Load("") for zero-config mode. If --config is set, call config.Load(path). Store loaded config for subcommands to access (use a package-level variable or cobra.Command annotation).
    2. Create `internal/cli/scan.go` with NewScanCmd():
       - Use: "scan", Short: "Discover and audit secrets in .env files"
       - RunE: Print "scan not yet implemented" and return nil (stub)
    3. Create `internal/cli/rotate.go` with NewRotateCmd():
       - Use: "rotate", Short: "Rotate a secret", Args: cobra.ExactArgs(1) for SECRET_NAME
       - RunE: Print "rotate not yet implemented" and return nil (stub)
    4. Create `internal/cli/status.go` with NewStatusCmd():
       - Use: "status", Short: "Show secret rotation status"
       - RunE: Print "status not yet implemented" and return nil (stub)
    5. Create `internal/cli/history.go` with NewHistoryCmd():
       - Use: "history", Short: "Show rotation audit log"
       - RunE: Print "history not yet implemented" and return nil (stub)
    6. Create `cmd/rotator/main.go` per research minimal main.go:
       - Import internal/cli, call NewRootCmd().Execute(), os.Exit(1) on error
    7. Verify the binary builds and runs: `go build -o rotator ./cmd/rotator && ./rotator --help`
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go build -o /tmp/rotator-test ./cmd/rotator && /tmp/rotator-test --help 2>&1 | grep -q "scan" && /tmp/rotator-test --help 2>&1 | grep -q "rotate" && /tmp/rotator-test --help 2>&1 | grep -q "status" && /tmp/rotator-test --help 2>&1 | grep -q "history" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>`rotator --help` shows all four subcommands (scan, rotate, status, history) with global flags (--config, --verbose, --dry-run). Binary builds with zero errors.</done>
</task>

</tasks>

<verification>
- `go test ./internal/config/... -v -count=1` -- all config tests pass
- `go build ./cmd/rotator` -- binary builds without errors
- `./rotator --help` -- shows scan, rotate, status, history subcommands
- `go vet ./...` -- no issues
</verification>

<success_criteria>
- Config loads from YAML file, validates, reports clear errors for invalid config
- Config works with no file (zero-config mode, DISC-03)
- Config supports env_files array for multi-file setups (DISC-04 config-level support)
- CLI binary runs, shows all 4 subcommands in help output
- All tests pass with `go test ./... -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
