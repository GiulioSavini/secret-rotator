---
phase: 03-rotation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/provider/provider.go
  - internal/provider/registry.go
  - internal/provider/password.go
  - internal/provider/generic.go
  - internal/provider/provider_test.go
  - internal/provider/password_test.go
  - internal/provider/generic_test.go
autonomous: true
requirements:
  - PROV-01

must_haves:
  truths:
    - "Provider interface defines Name, Rotate, Verify, Rollback methods"
    - "Registry maps type string to Provider and returns error for unknown types"
    - "Password generation produces cryptographically random URL-safe base64 strings"
    - "Generic provider generates a new password, returns Result with old+new, and has no DB interaction"
    - "Generic provider Verify is a no-op success (no external service to verify)"
    - "Generic provider Rollback is a no-op (rollback handled at engine level via .env restore)"
  artifacts:
    - path: "internal/provider/provider.go"
      provides: "Provider interface and ProviderConfig/Result types"
      exports: ["Provider", "ProviderConfig", "Result"]
    - path: "internal/provider/registry.go"
      provides: "Provider registry for type-based dispatch"
      exports: ["Registry", "NewRegistry", "Register", "Get"]
    - path: "internal/provider/password.go"
      provides: "Secure password generation"
      exports: ["GeneratePassword", "DefaultPasswordLength"]
    - path: "internal/provider/generic.go"
      provides: "Generic provider implementation"
      exports: ["GenericProvider"]
  key_links:
    - from: "internal/provider/generic.go"
      to: "internal/provider/provider.go"
      via: "GenericProvider implements Provider interface"
      pattern: "func.*GenericProvider.*Rotate|func.*GenericProvider.*Verify|func.*GenericProvider.*Rollback"
    - from: "internal/provider/generic.go"
      to: "internal/provider/password.go"
      via: "Rotate calls GeneratePassword"
      pattern: "GeneratePassword"
---

<objective>
Create the Provider interface contracts, registry, password generation, and Generic provider implementation.

Purpose: Establish the provider abstraction that all rotation providers implement, plus the simplest provider (Generic) that proves the interface works without any database interaction. This is the foundation for the execution engine and all database providers.
Output: internal/provider/ package with interface, registry, password gen, and working generic provider with tests.
</objective>

<execution_context>
@/home/giulio/.claude/get-shit-done/workflows/execute-plan.md
@/home/giulio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rotation-engine/03-RESEARCH.md

<interfaces>
<!-- Config types needed for provider dispatch -->

From internal/config/config.go:
```go
type SecretConfig struct {
    Name       string            `koanf:"name"`
    Type       string            `koanf:"type"`       // "generic", "mysql", "postgres", "redis"
    EnvKey     string            `koanf:"env_key"`
    EnvFile    string            `koanf:"env_file"`
    EnvFiles   []string          `koanf:"env_files"`
    Containers []string          `koanf:"containers"`
    Provider   map[string]string `koanf:"provider"`   // host, port, username, password, password_env, database
    Schedule   string            `koanf:"schedule"`
    Length     int               `koanf:"length"`      // password length for generic provider
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider interface, ProviderConfig, Result, Registry, and password generation</name>
  <files>internal/provider/provider.go, internal/provider/registry.go, internal/provider/password.go, internal/provider/provider_test.go, internal/provider/password_test.go</files>
  <behavior>
    - Registry.Get("generic") returns registered generic provider
    - Registry.Get("unknown") returns error "unknown provider: unknown"
    - Registry.Register panics or overwrites on duplicate (pick overwrite for simplicity)
    - GeneratePassword(32) returns a 43-char URL-safe base64 string (no padding)
    - GeneratePassword(0) uses DefaultPasswordLength (32 bytes)
    - GeneratePassword produces different results on successive calls
    - Generated passwords contain only [A-Za-z0-9_-] characters (RawURLEncoding)
  </behavior>
  <action>
    Create internal/provider/ package directory.

    **provider.go:** Define the Provider interface with four methods exactly as in research:
    - `Name() string`
    - `Rotate(ctx context.Context, cfg ProviderConfig, currentSecret string) (*Result, error)`
    - `Verify(ctx context.Context, cfg ProviderConfig, secret string) error`
    - `Rollback(ctx context.Context, cfg ProviderConfig, oldSecret string) error`

    Define ProviderConfig struct: Host, Port (int), Username, Database (string), Options (map[string]string).
    Define Result struct: OldSecret, NewSecret (string).

    **registry.go:** Registry struct with `providers map[string]Provider`. NewRegistry(), Register(p Provider), Get(name string) (Provider, error). Get returns `fmt.Errorf("unknown provider: %s", name)` on miss.

    **password.go:** `const DefaultPasswordLength = 32`. `GeneratePassword(length int) (string, error)` using crypto/rand + base64.RawURLEncoding. If length <= 0, use DefaultPasswordLength.

    Write tests FIRST (red), then implement (green).
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go test ./internal/provider/... -run "TestRegistry|TestGeneratePassword" -v -count=1</automated>
  </verify>
  <done>Provider interface exported, Registry dispatches by name, GeneratePassword produces crypto-random URL-safe passwords. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generic provider implementation</name>
  <files>internal/provider/generic.go, internal/provider/generic_test.go</files>
  <behavior>
    - GenericProvider.Name() returns "generic"
    - GenericProvider.Rotate(ctx, cfg, "oldpass") returns Result{OldSecret: "oldpass", NewSecret: <generated>}
    - GenericProvider.Rotate respects cfg password length from Options["length"] if set, otherwise DefaultPasswordLength
    - GenericProvider.Verify always returns nil (no service to verify)
    - GenericProvider.Rollback always returns nil (engine handles .env rollback)
    - GenericProvider.Rotate returns error if password generation fails (edge case)
  </behavior>
  <action>
    **generic.go:** Implement GenericProvider struct (empty). Methods:
    - `Name() string` returns "generic"
    - `Rotate(ctx, cfg, currentSecret)`: call GeneratePassword with length from cfg.Options["length"] (parse to int, default to 0 which triggers DefaultPasswordLength). Return &Result{OldSecret: currentSecret, NewSecret: generated}.
    - `Verify(ctx, cfg, secret)`: return nil (no external service for generic secrets)
    - `Rollback(ctx, cfg, oldSecret)`: return nil (engine handles .env restore + container restart)

    Write tests FIRST (red), then implement (green). Use testify/assert.
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go test ./internal/provider/... -run "TestGeneric" -v -count=1</automated>
  </verify>
  <done>GenericProvider implements Provider interface. Rotate generates a new password and returns old+new. Verify and Rollback are no-ops. All tests pass.</done>
</task>

</tasks>

<verification>
- `go test ./internal/provider/... -v -count=1` all pass
- `go vet ./internal/provider/...` clean
- Provider interface is exported and usable from other packages
- GenericProvider registered in registry successfully in tests
</verification>

<success_criteria>
- Provider interface with Name/Rotate/Verify/Rollback is defined and exported
- Registry maps string names to Provider implementations
- GeneratePassword produces crypto-random URL-safe base64 passwords
- GenericProvider implements Provider: generates password, no DB interaction
- All tests pass with `go test ./internal/provider/... -v -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/03-rotation-engine/03-01-SUMMARY.md`
</output>
