---
phase: 04-scheduling-and-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/status.go
  - internal/cli/status_test.go
autonomous: true
requirements: [CLI-03]

must_haves:
  truths:
    - "rotator status shows each managed secret's name, type, and provider"
    - "rotator status shows each secret's age (time since last rotation from history)"
    - "rotator status shows each secret's next scheduled rotation time (from cron expression in config)"
    - "rotator status works with --config flag and shows all configured secrets"
  artifacts:
    - path: "internal/cli/status.go"
      provides: "Status command implementation replacing stub"
      min_lines: 60
    - path: "internal/cli/status_test.go"
      provides: "Status command tests"
      min_lines: 40
  key_links:
    - from: "internal/cli/status.go"
      to: "internal/config/config.go"
      via: "Reads AppConfig.Secrets for secret definitions"
      pattern: "AppConfig\\.Secrets"
    - from: "internal/cli/status.go"
      to: "internal/history/store.go"
      via: "Reads history to determine last rotation time"
      pattern: "history\\.NewStore|store\\.List"
---

<objective>
Replace the status command stub with a full implementation showing secret rotation state, age, and schedules.

Purpose: Users need visibility into their managed secrets -- what is configured, when it last rotated, and when the next rotation is due. This is the operational dashboard for the CLI.
Output: Working `rotator status` command with tabular output.
</objective>

<execution_context>
@/home/giulio/.claude/get-shit-done/workflows/execute-plan.md
@/home/giulio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/cli/root.go
@internal/cli/status.go
@internal/cli/history.go
@internal/config/config.go
@internal/history/store.go
</context>

<interfaces>
<!-- Key types the executor needs. -->

From internal/config/config.go:
```go
type SecretConfig struct {
	Name       string            `koanf:"name"`
	Type       string            `koanf:"type"`
	EnvKey     string            `koanf:"env_key"`
	Schedule   string            `koanf:"schedule"`
	// ...other fields
}
```

From internal/history/store.go:
```go
func NewStore(path string, passphrase []byte) *Store
func (s *Store) List() ([]HistoryEntry, error)
```

From internal/history/types.go:
```go
type HistoryEntry struct {
	SecretName string    `json:"secret_name"`
	RotatedAt  time.Time `json:"rotated_at"`
	OldValue   string    `json:"old_value"`
	NewHash    string    `json:"new_hash"`
	Status     string    `json:"status"`
	Details    string    `json:"details"`
}
```

From internal/cli/history.go:
```go
func resolvePassphrase(explicit string) string  // already exists, reuse
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement status command</name>
  <files>internal/cli/status.go, internal/cli/status_test.go</files>
  <behavior>
    - Test 1: Status with no config shows "No secrets configured"
    - Test 2: Status with secrets in config shows table with NAME, TYPE, AGE, SCHEDULE, NEXT ROTATION columns
    - Test 3: Status shows "never" for age when no history entry exists for a secret
    - Test 4: Status shows human-readable duration (e.g., "3d 2h") for age when history exists
    - Test 5: Status shows "none" for schedule column when no cron expression configured
    - Test 6: Status shows next rotation time computed from cron expression when schedule is set
    - Test 7: Status shows summary line with total secret count
  </behavior>
  <action>
Replace `internal/cli/status.go` stub entirely. The new implementation:

1. Add `--passphrase` flag (same pattern as history command, reuse `resolvePassphrase`)
2. In RunE:
   - Check `AppConfig != nil && len(AppConfig.Secrets) > 0`, print "No secrets configured." and return if empty
   - If passphrase available, create history.Store and call List() to get all entries
   - Build a map of `secretName -> lastRotatedAt` by iterating entries and keeping the latest RotatedAt with status "success" per secret
   - For each secret in AppConfig.Secrets, compute:
     - `age`: if lastRotatedAt exists, format as human-readable duration (e.g., "3d 2h", "45m"); otherwise "never"
     - `schedule`: the cron expression string from SecretConfig.Schedule, or "none"
     - `nextRotation`: if schedule is set, parse with `cron.ParseStandard(schedule)` from robfig/cron/v3 and call `.Next(time.Now())` to get next fire time, format as "2006-01-02 15:04"; otherwise "-"
   - Output tabwriter table: NAME, TYPE, AGE, SCHEDULE, NEXT ROTATION
   - Print summary: "N secrets configured"

3. Create helper `formatDuration(d time.Duration) string` that formats:
   - <1h: "{m}m"
   - <1d: "{h}h {m}m"
   - >=1d: "{d}d {h}h"

Add `robfig/cron/v3` import for cron parsing (same dependency as Plan 01 Task 2 -- both plans are Wave 1 so either may add it first; `go get` is idempotent).

Tests: Use cobra command execution pattern (set AppConfig, capture stdout). For history-dependent tests, create a temp history file with known entries. Use `time.Now().Add(-72*time.Hour)` for predictable age values.
  </action>
  <verify>
    <automated>cd /home/giulio/secret-rotator && go test ./internal/cli/... -run TestStatus -count=1 -v</automated>
  </verify>
  <done>
- `rotator status` shows all configured secrets with name, type, age, schedule, and next rotation time
- Age is "never" when no rotation history exists, human-readable duration otherwise
- Schedule shows cron expression or "none"
- Next rotation computed from cron or shows "-"
- All tests pass
  </done>
</task>

</tasks>

<verification>
- `go test ./internal/cli/... -run TestStatus -count=1` passes
- `go vet ./internal/cli/...` is clean
- `go build ./cmd/rotator/` succeeds
- `./rotator status --help` shows the command with --passphrase flag
</verification>

<success_criteria>
- Status command replaces stub with full implementation
- Shows all secrets with age, schedule, and next rotation
- Handles edge cases: no config, no history, no schedule
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-and-operations/04-02-SUMMARY.md`
</output>
